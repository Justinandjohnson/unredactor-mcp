# Render Blueprint - https://render.com/docs/blueprint-spec
services:
  - type: web
    name: unredactor-mcp
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /mcp
